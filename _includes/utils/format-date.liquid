{% comment %}
  Format a date string
  
  Parameters:
  - date: The date to format (required)
  - format: The format string (optional, default: date_to_string)
  
  Available formats:
  - date_to_string: "01 Jan 2019"
  - date_to_long_string: "01 January 2019"
  - date_to_rfc822: "Tue, 01 Jan 2019 00:00:00 +0000"
  - Custom format: Use strftime format codes
  
  Output: formatted_date variable
{% endcomment %}

{% if include.format %}
  {% assign formatted_date = include.date | date: include.format %}
{% else %}
  {% assign formatted_date = include.date | date_to_string %}
{% endif %}
